# Representation-of-data
This Python project demonstrates data representation and visualization using NumPy, Pandas, Matplotlib, and Seaborn. It generates a dummy dataset with numerical and categorical features, then explores the data through raw values, DataFrames, descriptive statistics, histograms, scatter plots, box plots, and line plots for analysis and insights.
